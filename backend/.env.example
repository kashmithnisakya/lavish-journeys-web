# SendGrid Configuration
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# Make sure to verify your domain or sender email in SendGrid
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Verified sender email in SendGrid (must be verified in SendGrid dashboard)
SENDGRID_FROM_EMAIL=support@lavishtravelsandtours.online

# Support team email where customer inquiries will be sent
SUPPORT_EMAIL=kashmithnisakya@gmail.com

# Note: The following are hardcoded in app/config.py and don't need to be set here:
# - ALLOWED_ORIGINS (["*"])
# - APP_NAME, APP_VERSION, DEBUG, HOST, PORT
